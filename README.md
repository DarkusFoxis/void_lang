# 📖 Void Language — Полная документация

## Версия 1.0.1

---

# 📋 Оглавление

1. [Введение](#1-введение)
2. [Установка и запуск](#2-установка-и-запуск)
3. [Структура программы](#3-структура-программы)
4. [Стили написания (using style)](#4-стили-написания-using-style)
5. [Комментарии](#5-комментарии)
6. [Типы данных](#6-типы-данных)
7. [Переменные](#7-переменные)
8. [Операторы](#8-операторы)
9. [Ввод и вывод](#9-ввод-и-вывод)
10. [Условные конструкции](#10-условные-конструкции)
11. [Циклы](#11-циклы)
12. [Встроенные функции](#12-встроенные-функции)
13. [Приведение типов](#13-приведение-типов)
14. [Примеры программ](#14-примеры-программ)
15. [Ошибки и отладка](#15-ошибки-и-отладка)
16. [Справочная таблица](#16-справочная-таблица)

---

# 1. Введение

**Void** — это интерпретируемый язык программирования с простым и понятным синтаксисом. Разработан для обучения основам программирования. Интерпретатор написан на TypeScript.

### Ключевые особенности

- 🎯 **Простой синтаксис** — легко читать и писать
- 🎨 **Стили написания** — настройка внешнего вида кода
- 🔢 **Полная арифметика** — включая возведение в степень
- 📝 **Строгая типизация** — 4 базовых типа данных
- 🔄 **Управляющие конструкции** — условия, циклы
- 📦 **Встроенные функции** — математика, строки, конвертация
- 📁 **Расширение файлов** — `.void`

### Философия языка

```
"Из пустоты рождается код, из кода рождается смысл."
```

Название **Void** (пустота) символизирует чистый лист, с которого начинается каждая программа.

---

# 2. Установка и запуск

### Требования

- Node.js 16+
- npm или yarn

### Установка

```bash
# Клонировать репозиторий
git clone https://github.com/DarkusFoxis/void_lang
cd void-lang

# Установить зависимости
npm install

# Собрать проект
npm run build
```

### Запуск программы

```bash
# Через собранную версию
node dist/index.js программа.void

# Через ts-node (без сборки)
npx ts-node src/index.ts программа.void

# Помощь
node dist/index.js --help

# Версия
node dist/index.js --version
```

### Первая программа

Создайте файл `hello.void`:

```
@VoidApp "Hello";
using style "Abyss";

main()
{
    echo("Hello, Void!");
}

@VoidEnd;
```

Запустите:

```bash
node dist/index.js hello.void
```

---

# 3. Структура программы

Каждая программа на Void имеет строгую структуру:

```
┌─────────────────────────────────────┐
│  @VoidApp "ИмяПриложения";          │  ← Заголовок (обязательно)
│  using style "Стиль";               │  ← Стиль (необязательно)
│                                     │
│  main()                             │  ← Точка входа (обязательно)
│  {                                  │
│      // Ваш код здесь               │
│  }                                  │
│                                     │
│  @VoidEnd;                          │  ← Конец программы (обязательно)
│  (всё после игнорируется)           │
└─────────────────────────────────────┘
```

### 3.1 Заголовок программы — `@VoidApp`

Каждая программа **обязана** начинаться с директивы `@VoidApp`:

```
@VoidApp "НазваниеПриложения";
```

- Имя приложения указывается в **двойных кавычках**
- После имени — **точка с запятой**
- Имя используется для идентификации программы при запуске

**Примеры:**

```
@VoidApp "MyFirstApp";
@VoidApp "Calculator";
@VoidApp "Тестовая программа";
```

### 3.2 Стиль — `using style`

Необязательная директива, указывающая стиль написания кода:

```
using style "ИмяСтиля";
```

Подробнее о стилях — в разделе [4. Стили написания](#4-стили-написания-using-style).

### 3.3 Функция `main()`

Точка входа — обязательная функция `main()`:

```
main()
{
    // Весь исполняемый код здесь
}
```

- Скобки `()` после `main` обязательны
- Тело функции заключается в фигурные скобки `{ }`
- Выполнение программы начинается с первой инструкции внутри `main()`

### 3.4 Конец программы — `@VoidEnd`

Каждая программа **обязана** заканчиваться директивой:

```
@VoidEnd;
```

**Всё, что написано после `@VoidEnd;`, полностью игнорируется.** Это удобно для:

- Заметок разработчика
- Черновиков кода
- Документации

```
@VoidEnd;

Здесь можно писать что угодно.
Этот текст не будет обработан интерпретатором.
TODO: добавить новые функции
```

---

# 4. Стили написания (using style)

Стили в Void — это **философия и рекомендации по написанию кода**. Стиль задаёт тон и настроение программы. На данный момент стиль является декоративной директивой, но в будущих версиях может влиять на поведение интерпретатора.

### 4.1 Доступные стили

#### 🌑 `"Abyss"` — Бездна

```
using style "Abyss";
```

**Философия:** Минимализм и глубина. Код, рождённый из пустоты.

**Рекомендации:**
- Минимальное количество переменных
- Короткие имена
- Лаконичные конструкции
- Глубокая вложенность допустима

**Пример в стиле Abyss:**

```
@VoidApp "Deep";
using style "Abyss";

main()
{
    create:int x = 42;
    echo(x * x);
}

@VoidEnd;
```

---

#### 🌅 `"Dawn"` — Рассвет

```
using style "Dawn";
```

**Философия:** Ясность и чистота. Каждая строка — как первый луч солнца.

**Рекомендации:**
- Понятные имена переменных
- Обильные комментарии
- Одно действие — одна строка
- Избегать глубокой вложенности

**Пример в стиле Dawn:**

```
@VoidApp "Greeting";
using style "Dawn";

main()
{
    // Приветствие пользователя
    create:string userName = write("Введите ваше имя: ");
    
    // Формируем приветственное сообщение
    create:string greeting = "Доброе утро, " + userName + "!";
    
    // Выводим результат
    echo(greeting);
}

@VoidEnd;
```

---

#### 🌙 `"Twilight"` — Сумерки

```
using style "Twilight";
```

**Философия:** Баланс между светом и тьмой. Элегантность в каждой строке.

**Рекомендации:**
- Сбалансированный код
- Умеренные комментарии
- Группировка логических блоков
- Элегантные решения

**Пример в стиле Twilight:**

```
@VoidApp "Balance";
using style "Twilight";

main()
{
    create:int a = 15;
    create:int b = 27;

    #* Находим среднее арифметическое *#
    create:float average = (a + b) / 2;
    echo("Среднее:", average);
    
    if (average > 20)
    {
        echo("Выше порога");
    }
    else
    {
        echo("Ниже порога");
    }
}

@VoidEnd;
```

---

#### 🔥 `"Ember"` — Тлеющие угли

```
using style "Ember";
```

**Философия:** Интенсивность и мощь. Код, который горит.

**Рекомендации:**
- Плотный код
- Сложные выражения в одну строку
- Активное использование арифметики
- Максимум вычислений

**Пример в стиле Ember:**

```
@VoidApp "HeatCalc";
using style "Ember";

main()
{
    create:float result = ((2 ** 10) + sqrt(144)) * 3.14 / 2;
    echo("Результат:", result, "| Округлённо:", round(result));
    
    create:int i = 1;
    create:int sum = 0;
    while (i <= 100) { sum = sum + i. i = i + 1; }
    echo("Сумма 1..100:", sum);
}

@VoidEnd;
```

---

#### ❄️ `"Frost"` — Мороз

```
using style "Frost";
```

**Философия:** Холодная точность. Каждая переменная выверена.

**Рекомендации:**
- Строгая типизация всех переменных
- Явные преобразования типов
- Проверка всех условий
- Оборонительное программирование

**Пример в стиле Frost:**

```
@VoidApp "Precise";
using style "Frost";

main()
{
    create:string input = write("Введите число: ");
    create:float number = toFloat(input);
    
    create:bool isPositive = number > 0;
    create:bool isWhole = number == toFloat(toInt(number));
    
    if (isPositive && isWhole)
    {
        create:int wholeNumber = toInt(number);
        echo("Целое положительное число:", wholeNumber);
    }
    else if (isPositive && !isWhole)
    {
        echo("Дробное положительное число:", number);
    }
    else
    {
        echo("Отрицательное или ноль:", number);
    }
}

@VoidEnd;
```

---

#### 🌊 `"Ocean"` — Океан

```
using style "Ocean";
```

**Философия:** Плавность и поток. Код течёт как вода.

**Рекомендации:**
- Плавные переходы между блоками
- Использование пустых строк для разделения
- «Потоковый» стиль обработки данных
- Мягкие имена переменных

**Пример в стиле Ocean:**

```
@VoidApp "Flow";
using style "Ocean";

main()
{
    create:string wave = "Привет".

    create:int depth = 1;
    while (depth <= 5)
    {
        create:string current = wave + " ~";
        echo(current);

        wave = current.
        depth = depth + 1.
    }

    echo("");
    echo("Глубина достигнута.");
}

@VoidEnd;
```

---

#### 🌿 `"Verdant"` — Зелень

```
using style "Verdant";
```

**Философия:** Рост и развитие. Код, который растёт органично.

**Рекомендации:**
- Начинать с простого, усложнять постепенно
- Каждый блок — новый уровень
- Обучающий стиль
- Много примеров

---

#### ⚡ `"Storm"` — Шторм

```
using style "Storm";
```

**Философия:** Хаос и порядок. Мощные алгоритмы.

**Рекомендации:**
- Сложные алгоритмы
- Вложенные циклы
- Математические вычисления
- Оптимизация

---

### 4.2 Сводная таблица стилей

| Стиль | Символ | Философия | Ключевое качество |
|---|---|---|---|
| `Abyss` | 🌑 | Минимализм из пустоты | Лаконичность |
| `Dawn` | 🌅 | Ясность и чистота | Читаемость |
| `Twilight` | 🌙 | Баланс света и тьмы | Элегантность |
| `Ember` | 🔥 | Интенсивность и мощь | Плотность |
| `Frost` | ❄️ | Холодная точность | Строгость |
| `Ocean` | 🌊 | Плавность и поток | Текучесть |
| `Verdant` | 🌿 | Рост и развитие | Органичность |
| `Storm` | ⚡ | Хаос и порядок | Мощность |

### 4.3 Пользовательские стили

Можно указать **любое имя стиля**:

```
using style "MyCustomStyle";
using style "CompanyStandard";
using style "Хаос";
```

Интерпретатор принимает любое имя — это ваша личная метка.

### 4.4 Без стиля

Директива `using style` **необязательна**. Программа работает и без неё:

```
@VoidApp "NoStyle";

main()
{
    echo("Работает без стиля!");
}

@VoidEnd;
```

---

# 5. Комментарии

### 5.1 Однострочные комментарии

Начинаются с `//` и продолжаются до конца строки:

```
// Это комментарий
echo("Hello"); // Комментарий после кода
```

### 5.2 Многострочные комментарии

Заключаются между `#*` и `*#`:

```
#*
    Это многострочный комментарий.
    Он может занимать несколько строк.
    Удобен для описания алгоритмов.
*#
```

### 5.3 Рекомендации по комментариям

```
@VoidApp "CommentDemo";
using style "Dawn";

main()
{
    // ===== Блок инициализации =====
    create:int x = 10;    // Координата X
    create:int y = 20;    // Координата Y

    #*
        Вычисляем расстояние от начала координат
        по формуле: d = sqrt(x² + y²)
    *#
    create:float distance = sqrt(x ** 2 + y ** 2);
    
    echo("Расстояние:", distance);
}

@VoidEnd;
```

---

# 6. Типы данных

Void поддерживает **4 базовых типа данных**:

### 6.1 `string` — Строка

Текстовые данные, заключённые в двойные или одинарные кавычки.

```
create:string name = "Void Language";
create:string quote = "Он сказал: \"Привет!\"";
```

**Escape-последовательности:**

| Последовательность | Значение |
|---|---|
| `\n` | Новая строка |
| `\t` | Табуляция |
| `\r` | Возврат каретки |
| `\\` | Обратный слэш |
| `\"` | Двойная кавычка |
| `\'` | Одинарная кавычка |

**Конкатенация строк:**

```
create:string first = "Hello";
create:string second = " World";
create:string result = first + second.  // "Hello World";
```

**Конкатенация с другими типами:**

```
create:string msg = "Ответ: " + 42;      // "Ответ: 42"
create:string msg2 = "Пи = " + 3.14;     // "Пи = 3.14"
create:string msg3 = "Флаг: " + true;    // "Флаг: true"
```

### 6.2 `int` — Целое число

Целые числа без дробной части.

```
create:int age = 25;
create:int negative = -10;
create:int zero = 0;
create:int big = 1000000;
```

**Особенности:**
- При присваивании дробного числа — **округление вниз** (floor)
- Диапазон ограничен JavaScript Number

```
create:int x = 7;   // 7
create:int y = 3;    // Если присвоить 3.9 → будет 3
```

### 6.3 `float` — Число с плавающей точкой

Числа с дробной частью.

```
create:float pi = 3.14159;
create:float temperature = -15.5;
create:float small = 0.001;
```

**Особенности:**
- Стандартная точность IEEE 754
- Разделитель дробной части — **точка**

### 6.4 `bool` — Логический тип

Значения `true` (истина) или `false` (ложь).

```
create:bool isActive = true.
create:bool hasError = false.
```

**Таблица истинности для приведения:**

| Значение | bool |
|---|---|
| `0` | `false` |
| `0.0` | `false` |
| `""` (пустая строка) | `false` |
| `false` | `false` |
| `null` | `false` |
| Любое другое | `true` |

### 6.5 Сводная таблица типов

| Тип | Ключевое слово | Пример | Размер |
|---|---|---|---|
| Строка | `string` | `"Hello"` | Динамический |
| Целое число | `int` | `42` | 64-bit float (JS) |
| Дробное число | `float` | `3.14` | 64-bit float |
| Логический | `bool` | `true` / `false` | 1 значение |

---

# 7. Переменные

### 7.1 Создание переменных

**Синтаксис:**

```
create:тип имя = значение.
```

| Часть | Описание |
|---|---|
| `create:` | Ключевое слово создания переменной |
| `тип` | Один из: `string`, `int`, `float`, `bool` |
| `имя` | Имя переменной (буквы, цифры, `_`) |
| `=` | Оператор присваивания |
| `значение` | Выражение или литерал |
| `;` | **Точка с запятой в конце** (обязательна!) |

**Примеры:**

```
create:string name = "Void";
create:int count = 0;
create:float ratio = 0.75;
create:bool isReady = true;
```

### 7.2 Именование переменных

**Правила:**
- Начинается с **буквы** или `_`
- Содержит **буквы**, **цифры**, `_`
- **Регистрозависимые** (`name` ≠ `Name`)
- Не может совпадать с ключевыми словами

**Допустимые имена:**

```
create:int x = 1;
create:int myVariable = 2;
create:int _private = 3;
create:int player1Score = 100;
create:int MAX_VALUE = 999;
```

**Недопустимые имена:**

```
create:int 1variable = 1;   // Начинается с цифры
create:int my-var = 2;      // Содержит дефис
create:int main = 3;        // Ключевое слово
```

### 7.3 Инициализация выражениями

Переменные можно инициализировать любым выражением:

```
create:int sum = 10 + 20;
create:float area = 3.14 * 5 ** 2;
create:string greeting = "Hello, " + "World!";
create:bool isAdult = 25 >= 18;
create:int sqrtVal = toInt(sqrt(144));
```

### 7.4 Изменение переменных

После создания значение можно изменить (используется `;`):

```
create:int x = 10;
echo(x);        // 10

x = x + 5;
echo(x);        // 15

x = x * 2;
echo(x);        // 30
```

> ⚠️ **Важно:** Изменение сохраняет тип. Если переменная `int`, присваиваемое значение будет приведено к `int`.

### 7.5 Область видимости

Переменные имеют **блочную область видимости**:

```
create:int x = 10;

if (true)
{
    create:int y = 20;     // y видна только здесь
    echo(x);               // 10 — x видна из внешнего блока
    echo(y);               // 20
}

echo(x);                   // 10 — OK
// echo(y);                // ОШИБКА — y не определена здесь
```

### 7.6 Ввод данных в переменную

```
create:string name = write("Ваше имя: ");
create:string ageStr = write("Ваш возраст: ");
create:int age = toInt(ageStr);
```

---

# 8. Операторы

### 8.1 Арифметические операторы

| Оператор | Описание | Пример | Результат |
|---|---|---|---|
| `+` | Сложение | `5 + 3` | `8` |
| `-` | Вычитание | `5 - 3` | `2` |
| `*` | Умножение | `5 * 3` | `15` |
| `/` | Деление | `7 / 2` | `3.5` |
| `%` | Остаток от деления | `7 % 3` | `1` |
| `**` | Возведение в степень | `2 ** 10` | `1024` |

**Примеры:**

```
create:int a = 15;
create:int b = 4;

echo(a + b);    // 19
echo(a - b);    // 11
echo(a * b);    // 60
echo(a / b);    // 3.75
echo(a % b);    // 3
echo(a ** b);   // 50625
```

### 8.2 Конкатенация строк

Оператор `+` для строк выполняет **конкатенацию**:

```
create:string result = "Hello" + ", " + "World!";
echo(result);  // Hello, World!
```

Если хотя бы один операнд — строка, второй автоматически преобразуется:

```
echo("Число: " + 42);        // Число: 42
echo("Пи: " + 3.14);         // Пи: 3.14
echo("Ответ: " + true);      // Ответ: true
```

### 8.3 Операторы сравнения

| Оператор | Описание | Пример | Результат |
|---|---|---|---|
| `==` | Равно | `5 == 5` | `true` |
| `!=` | Не равно | `5 != 3` | `true` |
| `<` | Меньше | `3 < 5` | `true` |
| `>` | Больше | `5 > 3` | `true` |
| `<=` | Меньше или равно | `5 <= 5` | `true` |
| `>=` | Больше или равно | `5 >= 3` | `true` |

**Результат сравнения** — всегда `bool`:

```
create:bool isEqual = (10 == 10);     // true
create:bool isMore = (5 > 10);        // false
```

### 8.4 Логические операторы

| Оператор | Описание | Пример | Результат |
|---|---|---|---|
| `&&` | Логическое И | `true && false` | `false` |
| `\|\|` | Логическое ИЛИ | `true \|\| false` | `true` |
| `!` | Логическое НЕ | `!true` | `false` |

**Таблица истинности:**

| A | B | A && B | A \|\| B | !A |
|---|---|---|---|---|
| true | true | true | true | false |
| true | false | false | true | false |
| false | true | false | true | true |
| false | false | false | false | true |

**Пример:**

```
create:int age = 25;
create:bool hasLicense = true;

if (age >= 18 && hasLicense)
{
    echo("Можете водить!");
}
```

### 8.5 Унарные операторы

| Оператор | Описание | Пример | Результат |
|---|---|---|---|
| `-` | Унарный минус | `-42` | `-42` |
| `!` | Логическое отрицание | `!true` | `false` |

```
create:int x = 10;
echo(-x);        // -10
echo(-(-x));     // 10

create:bool flag = true;
echo(!flag);     // false
```

### 8.6 Приоритет операторов

От **высшего** к **низшему**:

| Приоритет | Оператор | Ассоциативность |
|---|---|---|
| 1 (высший) | `()` | — |
| 2 | `-` (унарный), `!` | Правая |
| 3 | `**` | **Правая** |
| 4 | `*`, `/`, `%` | Левая |
| 5 | `+`, `-` | Левая |
| 6 | `<`, `>`, `<=`, `>=` | Левая |
| 7 | `==`, `!=` | Левая |
| 8 | `&&` | Левая |
| 9 (низший) | `\|\|` | Левая |

**Примеры приоритета:**

```
echo(2 + 3 * 4);          // 14  (не 20)
echo((2 + 3) * 4);        // 20
echo(2 ** 3 ** 2);         // 512 (= 2^9, правая ассоциативность)
echo(true || false && false);  // true (&&  выше ||)
```

### 8.7 Скобки

Скобки `()` изменяют приоритет вычислений:

```
create:int a = (2 + 3) * (4 - 1);     // 15
create:int b = 2 + 3 * 4 - 1;         // 13
create:float c = (10.0 + 20.0) / 3.0; // 10.0
```

---

# 9. Ввод и вывод

### 9.1 Вывод — `echo()`

Выводит одно или несколько значений в консоль:

**Синтаксис:**

```
echo(выражение1, выражение2, ...);
```

**Примеры:**

```
// Одно значение
echo("Hello, World!");

// Несколько значений (разделяются пробелом)
echo("Имя:", "Void");
echo("Возраст:", 25);
echo("Пи:", 3.14, "| e:", 2.718);

// Выражения
echo(2 + 2);
echo("Результат:", 10 * 5 + 3);

// Переменные
create:string name = "User";
echo("Привет,", name, "!");
```

**Вывод:**

```
Hello, World!
Имя: Void
Возраст: 25
Пи: 3.14 | e: 2.718
4
Результат: 53
Привет, User !
```

### 9.2 Ввод — `write()`

Читает данные от пользователя с консоли:

**Синтаксис:**

```
write("подсказка")
```

`write()` — это **выражение**, которое возвращает **строку**.

**Примеры:**

```
// Простой ввод строки
create:string name = write("Ваше имя: ");

// Ввод числа (требуется преобразование)
create:string input = write("Введите число: ");
create:int number = toInt(input);

// Ввод дробного числа
create:float value = toFloat(write("Введите значение: "));
```

**Полный пример:**

```
@VoidApp "InputExample";
using style "Dawn";

main()
{
    create:string name = write("Как вас зовут? ");
    create:int age = toInt(write("Сколько вам лет? "));
    create:string city = write("Из какого вы города? ");
    
    echo("");
    echo("=== Ваша анкета ===");
    echo("Имя:", name);
    echo("Возраст:", age);
    echo("Город:", city);
    
    if (age >= 18)
    {
        echo("Статус: совершеннолетний");
    }
    else
    {
        echo("Статус: несовершеннолетний");
    }
}

@VoidEnd;
```

---

# 10. Условные конструкции

### 10.1 Простое условие — `if`

```
if (условие)
{
    // Код выполняется, если условие истинно
}
```

**Пример:**

```
create:int x = 10;

if (x > 5)
{
    echo("x больше 5");
}
```

### 10.2 Условие с альтернативой — `if-else`

```
if (условие)
{
    // Если истина
}
else
{
    // Если ложь
}
```

**Пример:**

```
create:int age = 15;

if (age >= 18)
{
    echo("Добро пожаловать!");
}
else
{
    echo("Доступ запрещён. Вам", 18 - age, "лет до совершеннолетия.");
}
```

### 10.3 Цепочка условий — `if-else if-else`

```
if (условие1)
{
    // Блок 1
}
else if (условие2)
{
    // Блок 2
}
else if (условие3)
{
    // Блок 3
}
else
{
    // Блок по умолчанию
}
```

**Пример:**

```
create:int score = 75;

if (score >= 90)
{
    echo("Отлично! (A)");
}
else if (score >= 80)
{
    echo("Хорошо (B)");
}
else if (score >= 70)
{
    echo("Удовлетворительно (C)");
}
else if (score >= 60)
{
    echo("Посредственно (D)");
}
else
{
    echo("Неудовлетворительно (F)");
}
```

### 10.4 Вложенные условия

```
create:int x = 10;
create:int y = 20;

if (x > 0)
{
    if (y > 0)
    {
        echo("Обе координаты положительные");
        echo("Первая четверть");
    }
    else
    {
        echo("x положительный, y отрицательный");
        echo("Четвёртая четверть");
    }
}
else
{
    echo("x отрицательный или ноль");
}
```

### 10.5 Сложные условия

```
create:int age = 25;
create:bool isStudent = true;
create:float income = 15000.50;

if (age >= 18 && age <= 30 && (isStudent || income < 20000))
{
    echo("Вы подходите под программу поддержки молодёжи!");
}
```

---

# 11. Циклы

### 11.1 Цикл `while`

Выполняет блок кода, **пока условие истинно**:

```
while (условие)
{
    // Тело цикла
}
```

**Пример — счётчик:**

```
create:int i = 1;
while (i <= 10)
{
    echo("Шаг:", i);
    i = i + 1;
}
```

**Пример — сумма чисел:**

```
create:int sum = 0;
create:int n = 1;
while (n <= 100)
{
    sum = sum + n;
    n = n + 1;
}
echo("Сумма 1..100 =", sum);  // 5050
```

### 11.2 Цикл `for`

Компактная запись цикла с инициализацией, условием и обновлением:

```
for (инициализация; условие; обновление)
{
    // Тело цикла
}
```

**Пример:**

```
for (create:int i = 0; i < 10; i = i + 1)
{
    echo(i);
}
```

**Пример — таблица умножения:**

```
create:int n = 7;
echo("Таблица умножения на", n);
for (create:int i = 1; i <= 10; i = i + 1)
{
    echo(n, "*", i, "=", n * i);
}
```

**Пример — степени двойки:**

```
for (create:int i = 0; i <= 16; i = i + 1)
{
    echo("2 ^", i, "=", 2 ** i);
}
```

### 11.3 Вложенные циклы

```
echo("Таблица Пифагора (5x5):");
for (create:int i = 1; i <= 5; i = i + 1)
{
    for (create:int j = 1; j <= 5; j = j + 1)
    {
        echo(i, "*", j, "=", i * j);
    }
    echo("---");
}
```

### 11.4 Защита от бесконечных циклов

Интерпретатор имеет **лимит в 1 000 000 итераций**. Если цикл превысит этот лимит, программа остановится с ошибкой:

```
// Это вызовет ошибку через 1 000 000 итераций
while (true)
{
    echo("Бесконечный цикл!");
}
```

```
[Runtime Error] Превышено максимальное число итераций (1000000).
Возможен бесконечный цикл.
```

---

# 12. Встроенные функции

### 12.1 Математические функции

| Функция | Описание | Пример | Результат |
|---|---|---|---|
| `abs(x)` | Модуль числа | `abs(-42)` | `42` |
| `sqrt(x)` | Квадратный корень | `sqrt(144)` | `12` |
| `floor(x)` | Округление вниз | `floor(3.7)` | `3` |
| `ceil(x)` | Округление вверх | `ceil(3.2)` | `4` |
| `round(x)` | Округление к ближайшему | `round(3.5)` | `4` |
| `min(a, b)` | Минимум из двух | `min(5, 3)` | `3` |
| `max(a, b)` | Максимум из двух | `max(5, 3)` | `5` |
| `random()` | Случайное число [0, 1) (Устаревшее) | `random()` | `0.7341...` |
| `rand()` | Случайное число [min, max) | `rand(0, 2000)` | `1234` |`

**Примеры:**

```
echo(abs(-15));              // 15
echo(sqrt(2));               // 1.4142135623730951
echo(floor(9.99));           // 9
echo(ceil(0.01));            // 1
echo(round(2.5));            // 3
echo(min(100, 200));         // 100
echo(max(100, 200));         // 200

// Случайное целое число от 1 до 100
create:int randomNum = toInt(random() * 100) + 1;
echo("Случайное:", randomNum);

// Случайное число от 1 до 15
create:int randomNew = toInt(rand(1, 15));
echo("Случайное:", randomNew);

// Расстояние между точками
create:float x1 = 3.0;
create:float y1 = 4.0;
create:float distance = sqrt(x1 ** 2 + y1 ** 2);
echo("Расстояние:", distance);  // 5
```

### 12.2 Функции преобразования типов

| Функция | Описание | Пример | Результат |
|---|---|---|---|
| `toInt(x)` | Преобразовать в целое | `toInt("42")` | `42` |
| `toFloat(x)` | Преобразовать в дробное | `toFloat("3.14")` | `3.14` |
| `toString(x)` | Преобразовать в строку | `toString(42)` | `"42"` |
| `toBool(x)` | Преобразовать в bool | `toBool(1)` | `true` |

**Примеры:**

```
// Строка → Число
create:string input = write("Введите число: ");
create:int number = toInt(input);
create:float decimal = toFloat(input);

// Число → Строка
create:int age = 25;
create:string ageStr = toString(age);
echo("Мне " + ageStr + " лет");

// Число → Bool
echo(toBool(0));      // false
echo(toBool(1));      // true
echo(toBool(-1));     // true
echo(toBool(""));     // false
echo(toBool("text")); // true
```

### 12.3 Строковые функции

| Функция | Описание | Пример | Результат |
|---|---|---|---|
| `length(s)` | Длина строки | `length("Hello")` | `5` |
| `upper(s)` | В верхний регистр | `upper("hello")` | `"HELLO"` |
| `lower(s)` | В нижний регистр | `lower("HELLO")` | `"hello"` |
| `trim(s)` | Убрать пробелы по краям | `trim("  hi  ")` | `"hi"` |
| `contains(s, sub)` | Содержит подстроку? | `contains("Hello", "ell")` | `true` |

**Примеры:**

```
create:string text = "  Hello, Void Language!  ";

echo("Оригинал:", text);
echo("Длина:", length(text));
echo("Верхний:", upper(text));
echo("Нижний:", lower(text));
echo("Обрезан:", trim(text));
echo("Содержит 'Void':", contains(text, "Void"));
echo("Содержит 'Python':", contains(text, "Python"));
```

**Вывод:**

```
Оригинал:   Hello, Void Language!  
Длина: 25
Верхний:   HELLO, VOID LANGUAGE!  
Нижний:   hello, void language!  
Обрезан: Hello, Void Language!
Содержит 'Void': true
Содержит 'Python': false
```

### 12.4 Сводная таблица всех функций

| Категория | Функция | Аргументы | Возвращает |
|---|---|---|---|
| **Математика** | `abs(x)` | 1 число | число |
| | `sqrt(x)` | 1 число | float |
| | `floor(x)` | 1 число | int |
| | `ceil(x)` | 1 число | int |
| | `round(x)` | 1 число | int |
| | `min(a, b)` | 2 числа | число |
| | `max(a, b)` | 2 числа | число |
| | `random()` | 0 | float [0,1) |
| **Конвертация** | `toInt(x)` | 1 любой | int |
| | `toFloat(x)` | 1 любой | float |
| | `toString(x)` | 1 любой | string |
| | `toBool(x)` | 1 любой | bool |
| **Строки** | `length(s)` | 1 строка | int |
| | `upper(s)` | 1 строка | string |
| | `lower(s)` | 1 строка | string |
| | `trim(s)` | 1 строка | string |
| | `contains(s, sub)` | 2 строки | bool |
| **Ввод/Вывод** | `echo(...)` | любое кол-во | void |
| | `write(prompt)` | 1 строка | string |

---

# 13. Приведение типов

### 13.1 Автоматическое приведение

При создании переменной значение **автоматически приводится** к указанному типу:

```
create:int x = 3.7;        // x = 3 (floor)
create:float y = 42;       // y = 42.0
create:string z = 100;     // z = "100"
create:bool flag = 1;      // flag = true
```

### 13.2 Явное приведение

Используйте функции преобразования:

```
create:string numStr = "42";

create:int num = toInt(numStr);         // 42
create:float dec = toFloat(numStr);     // 42.0
create:bool b = toBool(numStr);         // true
create:string s = toString(42);         // "42"
```

### 13.3 Правила конкатенации

Если один из операндов `+` — строка, результат — **строка**:

```
echo("Результат: " + 42);           // "Результат: 42"
echo(42 + " - ответ");              // "42 - ответ"
echo("" + 3.14);                    // "3.14"
```

Если оба операнда — числа, результат — **число**:

```
echo(10 + 20);                      // 30
echo(3.14 + 2.86);                  // 6.0
```

### 13.4 Ошибки приведения

```
create:int x = toInt("abc");    // x = 0 (NaN → 0 через ||)

// Прямое присваивание нечисловой строки в int — ОШИБКА
create:int y = "hello";        // Runtime Error!
```

---

# 14. Примеры программ

### 14.1 Калькулятор

```
@VoidApp "Calculator";
using style "Dawn";

main()
{
    echo("=== Калькулятор Void ===");
    echo("");
    
    create:float a = toFloat(write("Первое число: "));
    create:float b = toFloat(write("Второе число: "));
    
    echo("");
    echo("Результаты:");
    echo(a, "+", b, "=", a + b);
    echo(a, "-", b, "=", a - b);
    echo(a, "*", b, "=", a * b);
    
    if (b != 0)
    {
        echo(a, "/", b, "=", a / b);
        echo(a, "%", b, "=", a % b);
    }
    else
    {
        echo("Деление на ноль невозможно!");
    }
    
    echo(a, "**", b, "=", a ** b);
}

@VoidEnd;
```

### 14.2 Угадай число

```
@VoidApp "GuessGame";
using style "Storm";

main()
{
    echo("=== Угадай число! ===");
    
    create:int secret = toInt(random() * 100) + 1;
    create:int attempts = 0;
    create:bool guessed = false;
    
    while (!guessed)
    {
        create:int guess = toInt(write("Ваша догадка (1-100): "));
        attempts = attempts + 1;
        
        if (guess == secret)
        {
            guessed = true;
            echo("Правильно! Вы угадали за", attempts, "попыток!");
        }
        else if (guess < secret)
        {
            echo("Больше!");
        }
        else
        {
            echo("Меньше!");
        }
    }
}

@VoidEnd;
```

### 14.3 Числа Фибоначчи

```
@VoidApp "Fibonacci";
using style "Twilight";

main()
{
    create:int n = toInt(write("Сколько чисел Фибоначчи вывести? ")).
    
    create:int a = 0;
    create:int b = 1;
    create:int count = 0;
    
    echo("Числа Фибоначчи:");
    while (count < n)
    {
        echo(a);
        create:int temp = a + b;
        a = b;
        b = temp;
        count = count + 1;
    }
}

@VoidEnd;
```

### 14.4 Проверка простого числа

```
@VoidApp "PrimeCheck";
using style "Frost";

main()
{
    create:int num = toInt(write("Введите число: "));
    
    create:bool isPrime = true;
    
    if (num <= 1)
    {
        isPrime = false;
    }
    else if (num <= 3)
    {
        isPrime = true;
    }
    else
    {
        create:int divisor = 2;
        while (divisor <= toInt(sqrt(toFloat(num))))
        {
            if (num % divisor == 0)
            {
                isPrime = false;
            }
            divisor = divisor + 1;
        }
    }
    
    if (isPrime)
    {
        echo(num, "— простое число");
    }
    else
    {
        echo(num, "— составное число");
    }
}

@VoidEnd;
```

### 14.5 Конвертер температур

```
@VoidApp "TempConverter";
using style "Ocean";

main()
{
    echo("=== Конвертер температур ===");
    echo("1. Цельсий → Фаренгейт");
    echo("2. Фаренгейт → Цельсий");
    echo("");

    create:int choice = toInt(write("Выберите (1 или 2): "));
    create:float temp = toFloat(write("Введите температуру: "));

    if (choice == 1)
    {
        create:float result = temp * 9 / 5 + 32;
        echo(temp, "°C =", round(result * 100) / 100, "°F");
    }
    else if (choice == 2)
    {
        create:float result = (temp - 32) * 5 / 9;
        echo(temp, "°F =", round(result * 100) / 100, "°C");
    }
    else
    {
        echo("Неверный выбор!");
    }
}

@VoidEnd;
```

### 14.6 Рисование треугольника

```
@VoidApp "Triangle";
using style "Verdant";

main()
{
    create:int height = toInt(write("Высота треугольника: "));
    
    echo("");
    for (create:int i = 1; i <= height; i = i + 1)
    {
        create:string line = "".
        
        // Пробелы
        for (create:int s = 0; s < height - i; s = s + 1)
        {
            line = line + " ";
        }
        
        // Звёздочки
        for (create:int j = 0; j < 2 * i - 1; j = j + 1)
        {
            line = line + "*";
        }
        
        echo(line);
    }
}

@VoidEnd;
```

**Вывод (высота 5):**

```
    *
   ***
  *****
 *******
*********
```

---

# 15. Ошибки и отладка

### 15.1 Типы ошибок

#### Ошибки лексера `[Lexer Error]`

Возникают при разборе исходного кода на токены:

```
[Lexer Error] Неожиданный символ: '$' (строка 5, столбец 10)
[Lexer Error] Незакрытая строка (строка 3, столбец 1)
[Lexer Error] Незакрытый многострочный комментарий
[Lexer Error] Неизвестная директива: @VoidStart
```

#### Ошибки парсера `[Parser Error]`

Возникают при построении AST:

```
[Parser Error] Ожидался токен SEMICOLON, получен RBRACE (строка 7, столбец 1)
[Parser Error] Программа должна начинаться с @VoidApp
[Parser Error] Неожиданная инструкция: 'xyz'
[Parser Error] Ожидался тип переменной (string/int/float/bool)
```

#### Ошибки выполнения `[Runtime Error]`

Возникают при исполнении программы:

```
[Runtime Error] Переменная 'x' не определена
[Runtime Error] Переменная 'x' уже определена
[Runtime Error] Деление на ноль
[Runtime Error] Невозможно преобразовать 'abc' в число
[Runtime Error] Неизвестная функция: 'myFunc'
[Runtime Error] Функция 'sqrt' ожидает 1 аргумент(ов), получено 2
[Runtime Error] Превышено максимальное число итераций (1000000)
```

### 15.2 Частые ошибки и решения

| Ошибка | Причина | Решение |
|---|---|---|
| Забыта `;` после `create:` | `create:int x = 5` | `create:int x = 5;` |
| Забыта `;` после `echo()` | `echo("hi")` | `echo("hi");` |
| Забыт `@VoidEnd;` | Программа не завершена | Добавьте `@VoidEnd;` |
| `create:` без типа | `create: x = 5.` | `create:int x = 5.` |
| Переменная не создана | `echo(x);` без `create:` | Сначала `create:int x = 0.` |
| Двойное создание | `create:int x = 1.` дважды | Используйте `x = 2;` для изменения |
| Деление на ноль | `10 / 0` | Проверьте делитель через `if` |

### 15.3 Советы по отладке

1. **Используйте `echo()` для отладки:**

```
create:int x = someExpression;
echo("DEBUG x =", x);  // Временный вывод
```

2. **Проверяйте типы:**

```
create:string input = write("Число: ");
echo("Введено:", input, "тип: string");
create:int num = toInt(input);
echo("Преобразовано:", num, "тип: int");
```

3. **Упрощайте выражения:**

```
// Вместо сложного выражения
create:float result = sqrt(abs(x ** 2 - y ** 2)) / (2 * pi);

// Разбейте на шаги
create:float diff = x ** 2 - y ** 2;
create:float absDiff = abs(diff);
create:float sqrtVal = sqrt(absDiff);
create:float result = sqrtVal / (2 * pi);
```

---

# 16. Справочная таблица

### Ключевые слова

| Слово | Назначение |
|---|---|
| `@VoidApp` | Начало программы |
| `@VoidEnd` | Конец программы |
| `using` | Директива подключения |
| `style` | Указание стиля |
| `main` | Главная функция |
| `create:` | Создание переменной |
| `echo` | Вывод в консоль |
| `write` | Ввод с консоли |
| `if` | Условие |
| `else` | Альтернативная ветвь |
| `while` | Цикл с условием |
| `for` | Цикл со счётчиком |
| `string` | Тип: строка |
| `int` | Тип: целое число |
| `float` | Тип: дробное число |
| `bool` | Тип: логический |
| `true` | Логическая истина |
| `false` | Логическая ложь |

### Операторы

| Категория | Операторы |
|---|---|
| Арифметические | `+` `-` `*` `/` `%` `**` |
| Сравнения | `==` `!=` `<` `>` `<=` `>=` |
| Логические | `&&` `\|\|` `!` |
| Присваивание | `=` |

### Разделители

| Символ | Назначение |
|---|---|
| `;` | Конец инструкции |
| `;` | Конец объявления переменной |
| `,` | Разделитель аргументов |
| `( )` | Группировка / аргументы |
| `{ }` | Блок кода |
| `" "` | Строковый литерал |

### Escape-последовательности

| Последовательность | Символ |
|---|---|
| `\n` | Новая строка |
| `\t` | Табуляция |
| `\r` | Возврат каретки |
| `\\` | Обратный слэш |
| `\"` | Двойная кавычка |
| `\'` | Одинарная кавычка |

---

### 📝 Шаблон программы

```
@VoidApp "МоёПриложение";
using style "Abyss";

main()
{
    // Ваш код здесь
    echo("Hello, Void!");
}

@VoidEnd;
```

---

> **Void Language v1.0.1** — Из пустоты рождается код. 🌀